version: 2

seeds:
  - name: store
    config:
      schema: bronze_adventureworks
      column_types:
        businessentityid: integer
        storename: varchar
        salespersonid: integer
        modifieddate: timestamp
