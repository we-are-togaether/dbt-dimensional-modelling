version: 2

seeds:
  - name: countryregion
    config:
      schema: bronze_adventureworks
      column_types:
        countryregioncode: varchar
        modifieddate: timestamp
        name: varchar
